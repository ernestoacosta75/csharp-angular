import { AddRequiredDirective } from './add-required.directive';

describe('AddRequiredDirective', () => {
  it('should create an instance', () => {
    const directive = new AddRequiredDirective();
    expect(directive).toBeTruthy();
  });
});
