export interface CinemaDto {
    id?: string;
    name: string;
    latitude: number;
    longitude: number;
}

export interface CinemaEditDto {
    id?: string;
    name: string;
    latitude: number;
    longitude: number;
}